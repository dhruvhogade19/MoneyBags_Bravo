spring:
  datasource:
    url: jdbc:h2:mem:notification-local;MODE=Oracle;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=FALSE
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate

eureka:
  client:
    enabled: false

spring.cloud.discovery.enabled: false
