CREATE TABLE BILLS_AUDIT_LOG (AUDIT_ID VARCHAR2(36) PRIMARY KEY, AGGREGATE_ID VARCHAR2(100), ACTION VARCHAR2(80) NOT NULL, OUTCOME VARCHAR2(20) NOT NULL, CORRELATION_ID VARCHAR2(64) NOT NULL, OCCURRED_AT TIMESTAMP WITH TIME ZONE NOT NULL);
